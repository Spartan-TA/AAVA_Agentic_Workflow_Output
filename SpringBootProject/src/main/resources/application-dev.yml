spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/warehouse_db_dev
    username: warehouse_dev
    password: dev_pass
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
logging:
  level:
    root: DEBUG
    org.springframework: DEBUG
    com.warehouse.employee: TRACE