-- V2: Insert reference data for roles and departments
INSERT INTO department (name) VALUES ('Operations'), ('Logistics'), ('HR'), ('IT'), ('Maintenance');

-- Roles are typically handled in the application, but for demo purposes:
-- If you have a roles table, insert roles. Otherwise, roles are in code.
-- Example:
-- INSERT INTO role (name) VALUES ('ADMIN'), ('HR'), ('SUPERVISOR'), ('WORKER');
