Title: Project Scaffolding & Domain Setup - Configure Database Migration

User Story: As a developer, I want Flyway/Liquibase configured for database migrations so that schema changes are versioned and repeatable.

Acceptance Criteria:
Scenario: Database migration tool is configured
  Given the project is built
  When I start the application
  Then Flyway/Liquibase should apply the baseline migration without errors
  And future migrations should be detected and applied automatically

Additional Details:
Priority: High
Story Points: 3
Dependencies: Project repository initialized
Notes: Enables safe schema evolution; required for all DB-backed modules.