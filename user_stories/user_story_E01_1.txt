Title: Initialize Spring Boot Project Scaffolding

User Story: As a developer I want to initialize a Spring Boot (Maven) project with a standardized structure so that all modules can be developed consistently and efficiently.

Acceptance Criteria:
Scenario: Project scaffolding and initial setup
  Given I have cloned the repository
  When I run the build command
  Then the project should build successfully and run on port 8080
  And the base package structure for employee, scheduling, attendance, and safety modules should exist

Additional Details:
- Priority: High
- Story Points: 3
- Dependencies: None
- Notes: Ensure README contains build and run instructions; base packages must be created as per domain boundaries.